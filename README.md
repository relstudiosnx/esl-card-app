# Card App

## Local Run

To get the frontend running locally:

1. Clone this repo

2. Install dependencies 
    ```
    npm install
    ```
3. Start the application (this project uses create-react-app)
    ```
    npm start
    ```
4. Go to [http://localhost:3000](http://localhost:3000)



